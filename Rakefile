# frozen_string_literal: true
require 'sinatra/activerecord'
require "sinatra/activerecord/rake"
require_relative 'lib/blog_ar.rb'

require "bundler/gem_tasks"
task default: %i[]


desc "Start a pry console with environment required"
task :console do
    Pry.start
end